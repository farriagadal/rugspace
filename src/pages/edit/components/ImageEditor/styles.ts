import styled from 'styled-components'

export const Container = styled.div`
  display: grid;
  justify-content: center;
  margin: 100px 0;

  canvas {
    background: var(--color-1);
    box-shadow: 0px 30px 100px rgb(164 160 171 / 35%);
  }
`
